
quiz
    var progress = 5;

      /* i = questions answered */
    for (i = 0; i < 5, i++)

          /* quizQ is a single string; quizA is an ordered list */
        on quizCard display (quizQ[i], quizA[i]) pair,
        input userAnswer (userAnswer = # of li clicked),
        function hide(quizQ[i], quizA[i]),
        function checkAnswer(userAnswer) {
            if (userAnswer == quizA[i].correctA) {
                score++;
            }
            else {
                prepend( "Oops. ")
            },
        display quizA[i].correctA on quizCard,
        progress--;
        display funFact,
        display nextQ button,
        on button click, hide(quizA[i].correctA and funFact)

    /* end for loop */

    display on quizCard ("Your score was " + score + " out of 5.")
